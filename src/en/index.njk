<!doctype html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8" />
    <title>{{ en_site_metadata.title }}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content="{{ en_site_metadata.description }}" />
    <meta name="author" content="{{ en_site_metadata.author }}" />
    <meta name="og:url" content="{{ en_site_metadata.url }}" />
    <meta name="og:title" content="{{ en_site_metadata.title }}" />
    <meta name="og:description" content="{{ en_site_metadata.description }}" />
    <meta name="og:locale" content="en_US" />
    <meta name="twitter:card" content="summary" />
    <link rel="stylesheet" href="../styles/main.css" />
    <script src="https://identity.netlify.com/v1/netlify-identity-widget.js"></script>
  </head>
  <body>
    <div class="m-4">
      <h1 class="text-3xl">Site coming soon...</h1>
      <p><a href="{{ en_copy.locales_url }}">{{ en_copy.locales_label }}</a></p>
    </div>
  </body>
</html>
